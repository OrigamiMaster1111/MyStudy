uint16_t adc_val = 0;
uint32_t adc_vol = 0;

void setup(){
  Serial.begin(115200);
}

void loop(){
  adc_val = analogRead(18);
  Serial.println(adc_val);/*模拟信号的AD值*/

  adc_vol = analogReadMilliVolts(8);
  Serial.println(adc_vol);/*电压值（单位：毫伏）*/
}
