void setup() {
  ledcAttach(23, 1000, 10);
  ledcWrite(23, 128);
}

void loop() {
  for (int i = 0; i < 1024; ++i){
    ledcWrite(23, i);
    delay(2);
  }

  for (int i = 1024; i >= 0; --i){
    ledcWrite(23, i);
    delay(2);
  }
}

